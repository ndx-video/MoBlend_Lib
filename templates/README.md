# Templates

Each template lives in `templates/{category}/{template-id}/` with:

- `{template-id}.mo.blend`
- `{template-id}.manifest.json`
- `{template-id}.webp`

Run `python scripts/build_index.py` from repo root after adding or updating templates.